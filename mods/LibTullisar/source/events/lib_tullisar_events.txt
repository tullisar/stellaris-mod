namespace = lib_tullisar

# Installation Event. Sets mod global installation state.
country_event = {
	id = lib_tullisar.1
	hide_window = yes
	fire_only_once = yes
	trigger = {
		NOT = {
			has_global_flag = lib_tullisar_installed
		}
	}
	immediate = {
		set_global_flag = lib_tullisar_installed
		set_global_flag = lib_tullisar_version_1_0_0
	}
}

# Scenario Selection Event - Host Only 
country_event = {
	id = lib_tullisar.2
	hide_window = no
	is_triggered_only = yes
	trigger = {
		
	}
}